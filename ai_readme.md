# ai-sdlc

This project uses AI-Lore (v0.8 shape). Read `.ai-lore-ai-sdlc/ai_readme.md` — the
floor — and follow its instructions. All methodology artifacts live in
`.ai-lore-ai-sdlc/memory/blueprint/{verbs,processes,contracts}/`, `core/` subfolders
holding the out-of-the-box set.

Self-hosting note: this project's Payload at `process/` is the *canonical source* of
AI-Lore itself; the operating copy in `blueprint/` is generated from it. Sessions run
on the operating copy; releases ship the canonical one.
